// difference bet i ++  && ++ i

let a = 5;
let b = a++; // b is 5, i is 6 (post-increment)

let x = 3;
let c = ++x; // c is now 4, x is 4 (pre-increment)


//set built-in object that allows to store unique values of any type
// must be distinct

